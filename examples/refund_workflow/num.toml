[language]
version = "0.2.0"
compatibility = "minor"
manifest_schema = 1

[project]
name = "refund-workflow"
version = "0.2.0"
source = "src"
entry = "src/main.num"

[runtime]
workflow_store = "memory"
audit_store = "stdout"

[security]
policy_mode = "strict"
tenant_isolation = true
