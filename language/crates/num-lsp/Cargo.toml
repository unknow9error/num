[package]
name = "num-lsp"
version = "0.4.20"
edition = "2021"
license = "MIT"
description = "Language server boundary for the num language"

[dependencies]
num-compiler = { path = "../num-compiler" }

[lib]
path = "src/lib.rs"
