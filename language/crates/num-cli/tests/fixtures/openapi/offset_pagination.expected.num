module generated.catalog

// OpenAPI review-required permission candidates. Generated names are scaffolding; review scopes before wiring them into roles or service routes.
permission ListItems
// OpenAPI review-required policy scaffold for connector `catalogApi`:
// policy OpenApiCatalogApiDataSharing {
//     // Add narrow allow rules only after reviewing request/response fields and authentication requirements.
//     // Example: allow private UserInput -> catalogApi.<method>
// }

type CatalogItem {
    id: Text
    name: Text
}

type CatalogPage {
    items: List<CatalogItem>
    total: Int
}

connector catalogApi {
    // OpenAPI review-required permission candidate `ListItems` inferred from operationId; add it to roles/routes only after reviewing the imported operation
    // OpenAPI review-required policy placeholder for `list_items`: no obvious private/auth fields were detected; still review data classification before allowing connector calls
    // OpenAPI review-required pagination metadata for operation `list_items`: limit/offset pagination hints [parameter:limit, parameter:offset]; generated connector is not an executable paginated client yet
    list_items(limit: Int, offset: Int) -> CatalogPage
}
